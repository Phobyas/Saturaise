<!-- Root element of PhotoSwipe. Must have class pswp. -->
<link rel="preload" href="{{ 'vendor-zoom.css' | asset_url }}" as="style" onload="this.onload=null;this.rel='stylesheet'">
<noscript><link rel="stylesheet" href="{{ 'vendor-zoom.css' | asset_url }}"></noscript>

<div class="pswp" tabindex="-1" role="dialog" aria-hidden="true">
   <!-- Background of PhotoSwipe. -->
   <div class="pswp__bg"></div>
   <!-- Slides wrapper with overflow:hidden. -->
   <div class="pswp__scroll-wrap">
       <!-- Container that holds slides. -->
       <div class="pswp__container">
           <div class="pswp__item"></div>
           <div class="pswp__item"></div>
           <div class="pswp__item"></div>
       </div> 
       <!-- Default (PhotoSwipeUI_Default) interface on top of sliding area. Can be changed. -->
       <div class="pswp__ui pswp__ui--hidden">
         <!-- Zoom button removed -->
         <button class="pswp__button pswp__button--arrow--left" title="Previous (arrow left)">
           {% render 'snip-icons',
              wrapper: '.pswp__scroll-wrap',
              type: 'theme',
              icon: 'left-arrow',
              size: '16px',
              classes: 'm0',
              fill: 'var(--directional-text)',
              hover: 'var(--directional-hover)' %}
         </button>
         <button class="pswp__button pswp__button--arrow--right" title="Next (arrow right)">
           {% render 'snip-icons',
              wrapper: '.pswp__scroll-wrap',
              type: 'theme',
              icon: 'right-arrow',
              size: '16px',
              classes: 'm0',
              fill: 'var(--directional-text)',
              hover: 'var(--directional-hover)' %}
         </button>
         <button class="pswp__button pswp__button--close" title="Close (Esc)">
           {% render 'snip-icons',
              wrapper: '.pswp__scroll-wrap',
              type: 'theme',
              icon: 'close',
              size: '16px',
              classes: 'm0',
              fill: 'var(--directional-text)',
              hover: 'var(--directional-hover)' %}
         </button>
       </div>
   </div>
</div>

<style>
/* White background */
.pswp__bg {
  background: #ffffff !important;
}

/* White background for zoomed state */
.pswp--zoomed-in .pswp__bg {
  opacity: 1 !important; /* Fully opaque white background when zoomed */
}

/* Hide zoom button from UI */
.pswp__button--zoom {
  display: none !important;
}

/* Ensure zooming still works with gestures */
.pswp--zoom-allowed .pswp__img {
  cursor: zoom-in !important;
}

.pswp--zoomed-in .pswp__img {
  cursor: grab !important;
}
</style>